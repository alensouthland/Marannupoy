<style>
	body
	{
		text-align: center;
		background-image: url("https://static.pexels.com/photos/1240/road-street-blur-blurred.jpg");
		background-size: 1920px 1080px;
    background-repeat: no-repeat;
		color: white;
		font-family: sans-serif;
	}


	h2
	{
		color: white;
		font-size: 50px;
	}

  p
	{
		color: white;
		font-size: 50px;
	}

	.header
	{
		border: 3px solid #FFFFFF;
        border-radius: 8px;
		font-size: 35px;
		color: #4A148C;
	}

	.listing
	{
		padding: 5px 15px;
        display: inline-block;
        background-color: white;
        border: 3px solid #FFFFFF;
        border-radius: 8px;
        color: #4A148C;
        font-family: sans-serif;
    }

    .action-show
    {
    	text-decoration: none;
    	color: #2196F3;

    }

    th
    {
    	border: 3px solid #FFFFFF;
    	background-color: white;
    	padding: 13px 9px;
    	box-shadow: 0 8px 16px 0 rgba(0,0,0,0.1), 0 6px 20px 0 rgba(0,0,0,0.19);
    }

    .back-link
    {
      text-decoration: none;
      font-size: 15px;
      padding: 15px 20px;
      color: white;
      border-radius: 10px;
      text-align: center;
      font-family: sans-serif;
      background-color: transparent;
      border: 3px solid #FFFFFF;
      box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2), 0 6px 20px 0 rgba(0,0,0,0.19);
  }


  .back-link:hover
  {
      background-color: white;
      color: #212121;
      -webkit-transition-duration: 0.4s; /* Safari */
      transition-duration: 0.4s;
      cursor: pointer;
      box-shadow: 0 12px 16px 0 rgba(0,0,0,0.24), 0 17px 50px 0 rgba(0,0,0,0.19);
  }
</style>


<%= link_to("<< Back to list", {:action => 'list'}, :class => 'back-link') %>

<div class="lost delete">
<h2>Delete Lost</h2>

<%= form_for(:lost, :url => {:action => 'destroy', :id => @lost.id}) do |f| -%>

<p>Are u sure u wanna permenantly delete this lost?</p>

<p class="reference-name"><%== @lost.lost_item_name %></p>

<div class="form-buttons">
<%= submit_tag("Delete Lost", :class => 'back-link') %>
</div>

<% end %>
</div>
